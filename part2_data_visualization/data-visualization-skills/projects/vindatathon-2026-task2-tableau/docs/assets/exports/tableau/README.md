# Prepared Extracts

These files are generated by:

```text
projects/vindatathon-2026-task2-tableau/scripts/prepare_tableau_data.py
```

The small summary extracts and metadata are suitable for GitHub commits. Two generated fact extracts are intentionally ignored by the project `.gitignore` because they exceed GitHub's normal file-size limits:

- `orders_enriched.csv`
- `order_items_enriched.csv`

Regenerate them from the raw source data when needed. The raw source data is expected at:

```text
projects/data/
```

Scope guardrails:

- Part 2 historical business data only.
- No `sales_test.csv`.
- No `sample_submission.csv`.
- No `baseline.ipynb`.
- Predictive outputs are EDA signals, not Kaggle forecasting submissions.
