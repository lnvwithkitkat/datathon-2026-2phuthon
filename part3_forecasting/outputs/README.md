# Outputs

`submission.csv` is written here only after running all notebooks and setting this in the final cell of `notebooks/03_timeseries_models_ensemble_calibration.ipynb`:

```python
WRITE_SUBMISSION = True
```

No old or helper submission is included by default.
